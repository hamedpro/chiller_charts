1 - in dev script of package json -> may output.css not be created when App.jsx is trying to import that 
install nodemon as local dep
float numbers in js and when you add them to each other 

show a badge about that data is fake until user gives us a data_absolute_file_path 